const initialState= {
   
    
}
export const homePageReducer =(state = initialState, action)=>{
    switch(action.type){
       
        
        default: {
            return state
        }
    }
}